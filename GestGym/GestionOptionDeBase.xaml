<UserControl x:Class="GestGym.GestionOptionDeBase" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignHeight="400" d:DesignWidth="900" mc:Ignorable="d">
    <Grid>
        <DataGrid x:Name="OptionsDataGrid" Margin="10" AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="False" IsReadOnly="False" RowEditEnding="OptionsDataGrid_RowEditEnding">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Binding="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Header="Nom" />
                <DataGridTextColumn Width="250" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Header="Description" />
                <DataGridTextColumn Width="80" Binding="{Binding Prix, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Header="Prix" />
                <DataGridTextColumn Width="120" Binding="{Binding Horaire, UpdateSourceTrigger=PropertyChanged}" Header="Horaire" />
                <DataGridTextColumn Width="80" Binding="{Binding NbreSeance, UpdateSourceTrigger=PropertyChanged}" Header="Nombre de Séance" />
                <DataGridTextColumn Width="110" Binding="{Binding DateDebutDisponible, StringFormat=d}" Header="Date Début" />
                <DataGridTextColumn Width="110" Binding="{Binding DateFinDisponible, StringFormat=d}" Header="Date Fin" />
                <DataGridTextColumn Width="50" Binding="{Binding Statut, UpdateSourceTrigger=PropertyChanged}" Header="Statut (0=Inactif,1=Actif)" />


            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>